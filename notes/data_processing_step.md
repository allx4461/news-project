# первый этап работы: исследование и обработка данных 
### цель этапа
получить готовый набор данных, где строка соответствует моменту принятия решения: 
информация, `decision time -> entry at day's Open -> exit at day's Close` 

(дневная постановка выбрана поскольку yfinance не позволяет экстрактировать точную цену в определенный момент дня, только цену входа, выхода, high & low)
### план работ
1. сделать паспорт данных FNSPID, узнать
    - размер таблицы
    - информацию о столбцах
    - период дат 
    - информацию о пропусках/дубликатах
    - часовой пояс и вид timestamp
    - количество уникальных тикеров
    - количество уникальных источников 
2. аналогично узнать больше про yfinance 
    - даты и таймзона
    - время Open & Close
    - какие пропуски
    
3. конвертировать данные FNSPID из исходного пояса в часовой пояс UTC
4. обработать пропуски и дубликаты, оставить только акции NVDA
5. подготовить цены и торговый календарь 
    не допустить утечек информации, распределить информацию по торговым дням, 
    то есть для каждой новости определить `decision_date`
    по правилу `decision_time = 16.00 NYK` 
    - новость вышедшая до 16.00 доступна в тот же торговый день
    - новость после 16.00 относится к следующему торговому дню
    - нерабочие дни относятся к ближайшему следующему торговому дню
    decision_date -> next_trading_date
6. применить семантический анализ, слить данные по дате принятия решения по правилу из конфига (`mean`)
7. получить единый датасет с open/close/high/low на каждую дату принятия решения
8. посчитать `target return = close[next_day]/open[next_day] - 1`


### структура 
в папке `/scripts` будут скрипты `data_exploration` для составления паспортов данных, `download_data` для потоковой загрузки датасета (если делать все 15гб разом, произойдет переполнение оперативной памяти),`filter_data` для обработки пропусков и правильного датирования,`assemble_data` для сборки итогового датасета `новости + цены`

### ход работы
1. получена информация о датасете:
```
rows: 15549299
cols: ['Unnamed: 0', 'Date', 'Article_title', 'Stock_symbol', 'Url', 'Publisher', 'Author', 'Article', 'Lsa_summary', 'Luhn_summary', 'Textrank_summary', 'Lexrank_summary']
dates: 1914-09-16 00:00:00+00:00 to 2024-01-09 00:00:00+00:00
missing:
{'Unnamed: 0': 13057514, 'Date': 0, 'Article_title': 1, 'Stock_symbol': 9804627, 'Url': 686, 'Publisher': 11522656, 'Author': 14362984, 'Article': 11809209, 'Lsa_summary': 13057522, 'Luhn_summary': 13057521, 'Textrank_summary': 13057521, 'Lexrank_summary': 13057521}
```
2. отфильтрованы по тикеру и сохранены только usecols-колонки, удалены колонки без дат публикации/названий статьи, получена информация об очищенном датасете:
```
rows: 11862
cols: ['Date', 'Article_title', 'Stock_symbol']
dates: 2011-03-03 00:00:00+00:00 to 2023-12-16 20:00:00+00:00
missing:
{'Date': 0, 'Article_title': 0, 'Stock_symbol': 0}
```
3. новости агрегированы по дате принятия решения (добавлен столбец с датой, но новости не сгруппированы)

в ходе работы я решила решать вопрос, что делать, если на одну дату принятия решения выпадает несколько новостей, следующим образом: 
в `nvda_news.csv` с определенной датой решения сначала применяем сентимент анализ (берем только значение "позитив-негатив"), затем считаем `mean` этого скора (метод подсчета итогового значения положу в конфиг) 

4. в `nvda_news.csv` добавлен столбец сентимент анализа финбертом, получен файл `nvda_sentiment.csv`. 

5. новости сгруппированы по дате принятия решения, сентимент скор посчитан для каждого дня как `mean`. 

6. подгружены и добавлены в датасет колонки из yfinance `nvda_prices_aggregated`: `Open      High       Low     Close      Volume`, 

7. посчитан по формуле таргет и добавлен в датасет. 

для полноценного датасета останется только сгенерировать фичи. 